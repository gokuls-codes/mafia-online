import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ["mafia.gokulkannanr.in"],
};

export default nextConfig;

/*

when jester, 1 mafia, 1 villager are there - no one can win

mayor save jester, jester didnt win, but jester got eliminated



*/
